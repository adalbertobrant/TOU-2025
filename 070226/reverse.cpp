#include <bits/stdc++.h>

using namespace std;
// locale  como colocar acento

int main() {
    string frase = "Isso eh uma frase";
    reverse( frase.begin(), frase.end() );
    cout << frase << endl;
    reverse( frase.begin(), frase.end() );
    cout << frase << endl;

	return 0;
}

